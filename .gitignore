# Claude Code personal settings (not shared with team)
.claude/settings.local.json

# Azure credentials (never commit these)
.env
*.env.local

# Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/terraform.tfvars

# Azure Functions local settings (contém strings de conexão — nunca commitar)
api/local.settings.json
api/node_modules/

# OS
.DS_Store
Thumbs.db
